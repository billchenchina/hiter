import 'package:flutter/material.dart';

class BBSPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('BBS'),),
      body: Center(
        child: Text('BBSPage'),
      ),
    );
  }
}
